package master;

public class Observer {
    @Override
    public String toString() {
        return "Классу Observer доступны переменные с модифакотрами доступа: " + Master.str2 + Master.str3 + Master.str4;
    }
}
